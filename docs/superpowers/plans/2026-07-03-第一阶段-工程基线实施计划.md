# 第一阶段工程基线实施计划

> **给执行代理的要求：** 实施本计划时必须使用 `superpowers:subagent-driven-development`（推荐）或 `superpowers:executing-plans`。请按任务逐项执行，所有步骤使用 checkbox (`- [ ]`) 跟踪。

**目标：** 通过质量脚本、CI 质量门、零 warning lint、浏览器烟测和 bundle 体积观察，让宠迹 AI 进入可持续迭代状态。

**架构：** 不改变现有产品行为。围绕现有 Vite/React 项目增加工具链，把 Fast Refresh 组件文件里的非组件导出迁出，并增加一组覆盖本地 demo 主闭环的 Playwright 烟测。

**技术栈：** Vite 8、React 19、TypeScript、npm、oxlint、自定义 Node 回归脚本、Playwright Chromium、GitHub Actions。

---

## 范围确认

本计划只实施 `docs/superpowers/specs/2026-07-03-宠迹-ai-产品化设计.md` 中的阶段 1。

包含：
- CI 质量门。
- 当前 lint warning 修复。
- 当前 demo 主闭环的浏览器烟测。
- bundle 体积报告。
- 质量命令文档。

不包含：
- Store slice 拆分。
- LocalStorage migration。
- 后端或 edge AI gateway。
- 桌面端布局重构。
- 共享产品数据、账号、审核或 analytics。

## 文件结构

修改：
- `package.json`：增加 quality、regression、E2E、bundle report 脚本。
- `package-lock.json`：安装 `@playwright/test` 后记录依赖。
- `.github/workflows/deploy.yml`：部署前运行生产质量命令。
- `src/components/ui/Toast.tsx`：组件文件只导出 `ToastProvider`。
- `src/pages/AiPage/components/WelcomeHero.tsx`：建议数据改为文件内部常量。
- `src/pages/MapPage/components.tsx`：移出 `useMapPlaces` hook。
- `src/pages/MapPage/index.tsx`：从新的 hook 文件导入 `useMapPlaces`。
- `src/lib/ai.ts`：移除未使用的 catch 参数。
- `README.md`：补充质量命令说明。

新增：
- `.github/workflows/quality.yml`：PR 和 main 分支质量工作流。
- `playwright.config.ts`：Playwright 配置，自动启动 Vite dev server。
- `tests/e2e/core-loop.spec.ts`：主闭环浏览器烟测。
- `scripts/bundle-report.mjs`：bundle budget 和最大产物报告。
- `src/components/ui/toast-context.ts`：Toast context、类型和 hooks。
- `src/pages/MapPage/useMapPlaces.ts`：地图地点 hook。

## 任务 1：增加质量脚本和 Playwright 配置

**文件：**
- 修改：`package.json`
- 修改：`package-lock.json`
- 新增：`playwright.config.ts`

- [ ] **步骤 1：安装 Playwright 测试依赖**

运行：

```bash
npm install -D @playwright/test
```

预期：
- `package.json` 的 `devDependencies` 包含 `@playwright/test`。
- `package-lock.json` 已更新。
- 命令退出码为 0。

- [ ] **步骤 2：替换 package scripts**

将 `package.json` 的 `scripts` 改为：

```json
"scripts": {
  "dev": "vite",
  "build": "tsc -b && vite build && node scripts/copy404.js",
  "lint": "oxlint",
  "test": "npm run test:regression",
  "test:regression": "node scripts/loop1-regression.mjs",
  "test:e2e": "playwright test",
  "test:e2e:headed": "playwright test --headed",
  "bundle:report": "node scripts/bundle-report.mjs",
  "quality": "npm run lint && npm run test:regression && npm run build && npm run bundle:report",
  "quality:full": "npm run quality && npm run test:e2e",
  "preview": "vite preview"
}
```

- [ ] **步骤 3：新增 Playwright 配置**

创建 `playwright.config.ts`：

```ts
import { defineConfig, devices } from '@playwright/test'

export default defineConfig({
  testDir: './tests/e2e',
  timeout: 30_000,
  expect: {
    timeout: 10_000,
  },
  fullyParallel: true,
  reporter: process.env.CI ? [['list']] : [['list'], ['html', { open: 'never' }]],
  use: {
    baseURL: 'http://127.0.0.1:5181',
    trace: 'on-first-retry',
  },
  webServer: {
    command: 'npm run dev -- --host 127.0.0.1',
    url: 'http://127.0.0.1:5181/pettrace-ai/',
    reuseExistingServer: !process.env.CI,
    timeout: 120_000,
  },
  projects: [
    {
      name: 'chromium-mobile',
      use: {
        ...devices['Desktop Chrome'],
        viewport: { width: 393, height: 852 },
      },
    },
  ],
})
```

- [ ] **步骤 4：确认现有非浏览器检查仍可运行**

运行：

```bash
npm run test:regression
npm run lint
```

预期：
- 回归脚本退出码为 0。
- 在任务 2 修复前，lint 仍会打印当前 Fast Refresh 和 unused catch warning。

- [ ] **步骤 5：提交任务 1**

```bash
git add package.json package-lock.json playwright.config.ts
git commit -m "chore: add quality scripts and playwright config"
```

## 任务 2：修复当前 lint warning，不关闭规则

**文件：**
- 新增：`src/components/ui/toast-context.ts`
- 修改：`src/components/ui/Toast.tsx`
- 修改：`src/pages/AiPage/ChatView.tsx`
- 修改：`src/pages/SettingsPage/index.tsx`
- 修改：`src/pages/PetPage/actions.ts`
- 修改：`src/pages/PetPage/hooks.ts`
- 修改：`src/pages/CommunityPage/PostDetailPage.tsx`
- 修改：`src/pages/CommunityPage/hooks.ts`
- 修改：`src/pages/MapPage/tiles.tsx`
- 修改：`src/pages/AiPage/components/WelcomeHero.tsx`
- 新增：`src/pages/MapPage/useMapPlaces.ts`
- 修改：`src/pages/MapPage/components.tsx`
- 修改：`src/pages/MapPage/index.tsx`
- 修改：`src/lib/ai.ts`

- [ ] **步骤 1：确认 warning 基线**

运行：

```bash
npm run lint
```

修复前预期 warning：
- `src/components/ui/Toast.tsx`：hook 导出触发 Fast Refresh warning。
- `src/lib/ai.ts`：catch 参数未使用。
- `src/pages/AiPage/components/WelcomeHero.tsx`：导出的建议数据触发 Fast Refresh warning。
- `src/pages/MapPage/components.tsx`：导出的 hook 触发 Fast Refresh warning。

- [ ] **步骤 2：把 Toast 类型和 hooks 移到非组件模块**

创建 `src/components/ui/toast-context.ts`：

```ts
import { createContext, useCallback, useContext } from 'react'

export type ToastKind = 'info' | 'success' | 'error' | 'warning' | 'ok' | 'warn'

export interface ToastCtx {
  show: (message: string, opts?: { kind?: ToastKind; title?: string; duration?: number }) => void
  dismiss: (id: number) => void
}

export const ToastContext = createContext<ToastCtx | null>(null)

export function useToast() {
  const value = useContext(ToastContext)
  if (!value) throw new Error('useToast must be used inside ToastProvider')
  return value
}

export function useSafeToast(): ToastCtx {
  const value = useContext(ToastContext)
  const show = useCallback(
    (message: string, opts?: { kind?: ToastKind; title?: string; duration?: number }) => {
      if (value) value.show(message, opts)
    },
    [value],
  )
  return {
    show,
    dismiss: value?.dismiss ?? (() => {}),
  }
}
```

- [ ] **步骤 3：让 `Toast.tsx` 只导出 Provider 组件**

在 `src/components/ui/Toast.tsx` 中，将顶部 import 改为：

```ts
import { useCallback, useMemo, useRef, useState } from 'react'
import { ToastContext, type ToastCtx, type ToastKind } from './toast-context'
```

删除 `Toast.tsx` 中的 `ToastKind`、`ToastCtx`、`Ctx` 定义，以及底部 `useToast`、`useSafeToast` 导出。

将 provider 标签从：

```tsx
<Ctx.Provider value={ctx}>
```

改为：

```tsx
<ToastContext.Provider value={ctx}>
```

将关闭标签同步改为：

```tsx
</ToastContext.Provider>
```

- [ ] **步骤 4：更新 Toast hook import**

按以下方式更新：

```ts
// src/pages/AiPage/ChatView.tsx
import { useToast } from '../../components/ui/toast-context'

// src/pages/SettingsPage/index.tsx
import { ToastProvider } from '../../components/ui/Toast'
import { useToast } from '../../components/ui/toast-context'

// src/pages/PetPage/actions.ts
import type { ToastKind } from '../../components/ui/toast-context'

// src/pages/PetPage/hooks.ts
export { useToast } from '../../components/ui/toast-context'

// src/pages/CommunityPage/PostDetailPage.tsx
import { useToast } from '../../components/ui/toast-context'

// src/pages/CommunityPage/hooks.ts
export { useToast } from '../../components/ui/toast-context'

// src/pages/MapPage/tiles.tsx
import { useToast } from '../../components/ui/toast-context'
```

`src/App.tsx` 继续从 `./components/ui/Toast` 导入 `ToastProvider`。

- [ ] **步骤 5：WelcomeHero 的建议数据改为内部常量**

在 `src/pages/AiPage/components/WelcomeHero.tsx` 中，将：

```ts
export interface Suggestion {
```

改为：

```ts
interface Suggestion {
```

将：

```ts
export const QUICK_SUGGESTIONS: Suggestion[] = [
```

改为：

```ts
const QUICK_SUGGESTIONS: Suggestion[] = [
```

- [ ] **步骤 6：把 `useMapPlaces` 移到 hook 文件**

创建 `src/pages/MapPage/useMapPlaces.ts`：

```ts
import { useStore } from '../../store/useStore'
import { PLACES } from '../../data/mock'
import { enrichPlace, type PlaceRich } from './filter'

export type Place = PlaceRich

export function useMapPlaces(): Place[] {
  const city = useStore((state) => state.city)
  const storePlaces = useStore((state) => state.places)
  const raw = PLACES[city]
  return raw.map((place) => enrichPlace(storePlaces[place.id] ?? place, place.city))
}
```

在 `src/pages/MapPage/components.tsx` 中移除：

```ts
import { PLACES } from '../../data/mock'
import { enrichPlace, type CategoryFilter, type SizeFilter, type PlaceRich } from './filter'
```

替换为：

```ts
import { type CategoryFilter, type SizeFilter, type PlaceRich } from './filter'
```

删除 `components.tsx` 中导出的 `useMapPlaces` 函数。

在 `src/pages/MapPage/index.tsx` 中，将：

```ts
import { useMapPlaces, ListContent, type CategoryFilter, type SizeFilter, type Place } from './components'
```

改为：

```ts
import { ListContent, type CategoryFilter, type SizeFilter, type Place } from './components'
import { useMapPlaces } from './useMapPlaces'
```

- [ ] **步骤 7：移除未使用的 catch 参数**

在 `src/lib/ai.ts` 中，将：

```ts
    } catch (e) {
      throw new AiFetchError('AI 返回无法解析为 JSON')
    }
```

改为：

```ts
    } catch {
      throw new AiFetchError('AI 返回无法解析为 JSON')
    }
```

- [ ] **步骤 8：验证 lint 清零**

运行：

```bash
npm run lint
npm run test:regression
```

预期：
- `npm run lint` 退出码为 0，且没有 warning。
- `npm run test:regression` 退出码为 0。

- [ ] **步骤 9：提交任务 2**

```bash
git add src/components/ui/toast-context.ts src/components/ui/Toast.tsx src/pages/AiPage/ChatView.tsx src/pages/SettingsPage/index.tsx src/pages/PetPage/actions.ts src/pages/PetPage/hooks.ts src/pages/CommunityPage/PostDetailPage.tsx src/pages/CommunityPage/hooks.ts src/pages/MapPage/tiles.tsx src/pages/AiPage/components/WelcomeHero.tsx src/pages/MapPage/useMapPlaces.ts src/pages/MapPage/components.tsx src/pages/MapPage/index.tsx src/lib/ai.ts
git commit -m "chore: resolve lint warnings"
```

## 任务 3：增加 GitHub Actions 质量门

**文件：**
- 新增：`.github/workflows/quality.yml`
- 修改：`.github/workflows/deploy.yml`

- [ ] **步骤 1：新增 PR 和 main 分支质量工作流**

创建 `.github/workflows/quality.yml`：

```yaml
name: Quality

on:
  pull_request:
  push:
    branches: [main]

jobs:
  verify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright Chromium
        run: npx playwright install --with-deps chromium

      - name: Run full quality suite
        run: npm run quality:full
```

- [ ] **步骤 2：部署前运行生产质量命令**

在 `.github/workflows/deploy.yml` 中，将：

```yaml
      - name: Build
        run: npm run build
```

改为：

```yaml
      - name: Quality checks and build
        run: npm run quality
```

- [ ] **步骤 3：用文本检查确认 workflow 已更新**

运行：

```bash
Select-String -Path .github\workflows\quality.yml -Pattern "npm run quality:full"
Select-String -Path .github\workflows\deploy.yml -Pattern "npm run quality"
```

预期：
- 第一条命令打印 `Run full quality suite` 对应命令行。
- 第二条命令打印 `Quality checks and build` 对应命令行。

- [ ] **步骤 4：提交任务 3**

```bash
git add .github/workflows/quality.yml .github/workflows/deploy.yml
git commit -m "ci: add quality gate"
```

## 任务 4：增加当前 demo 主闭环浏览器烟测

**文件：**
- 新增：`tests/e2e/core-loop.spec.ts`

- [ ] **步骤 1：新增 Playwright 主闭环用例**

创建 `tests/e2e/core-loop.spec.ts`：

```ts
import { expect, test } from '@playwright/test'

test.beforeEach(async ({ page }) => {
  await page.goto('/pettrace-ai/ai')
  await page.evaluate(() => localStorage.clear())
  await page.reload()
})

test('AI 规划可以跳转到地图地点', async ({ page }) => {
  await expect(page.getByText(/今天想带/)).toBeVisible()

  await page.getByPlaceholder('告诉AI你想去哪里...').click()
  await expect(page.getByText('PetTrace AI')).toBeVisible()

  await page.getByPlaceholder('告诉我你的目的地').fill('带金毛去上海玩一天，晚餐要室内可进')
  await page.getByRole('button', { name: '发送' }).click()

  const routeButton = page.getByRole('button', { name: /查看路线/ }).first()
  await expect(routeButton).toBeVisible()
  await routeButton.click()

  await expect(page).toHaveURL(/\/pettrace-ai\/map\?place=/)
  await expect(page.locator('[data-map-search]')).toBeVisible()
  await expect(page.getByPlaceholder('搜索宠物友好地点...')).toBeVisible()
})

test('可以基于本地数据发布社区验证', async ({ page }) => {
  await page.getByRole('tab', { name: '社区' }).click()
  await expect(page.getByRole('heading', { name: '社区' })).toBeVisible()

  await page.getByRole('button', { name: /发布验证/ }).click()
  await page.getByRole('button', { name: /打卡/ }).first().click()
  await page
    .getByPlaceholder('今天我也去了这家，带金毛室内坐不挤...')
    .fill('E2E 验证：现场规则清晰，牵绳即可进入户外区。')
  await page.getByRole('button', { name: '发布' }).click()

  await expect(page.getByText('发布成功')).toBeVisible()
  await expect(page.getByText(/E2E 验证/)).toBeVisible()
})

test('设置可以通过 LocalStorage 重新水合', async ({ page }) => {
  await page.getByRole('tab', { name: '设置' }).click()
  await expect(page.getByText('AI 服务')).toBeVisible()

  await page.locator('#api-key').fill('sk-e2e-local')
  await page.locator('#ai-model').fill('e2e-model')
  await page.waitForTimeout(500)
  await page.reload()

  await expect(page.locator('#api-key')).toHaveValue('sk-e2e-local')
  await expect(page.locator('#ai-model')).toHaveValue('e2e-model')
})
```

- [ ] **步骤 2：本地安装 Playwright Chromium**

运行：

```bash
npx playwright install chromium
```

预期：
- Chromium 浏览器安装完成。
- 命令退出码为 0。

- [ ] **步骤 3：运行 E2E**

运行：

```bash
npm run test:e2e
```

预期：
- Playwright 自动在 5181 端口启动 Vite dev server。
- 3 条测试全部通过。
- 第一阶段只以可见 UI 和 URL 断言作为失败标准，不强制检查 console error。

- [ ] **步骤 4：提交任务 4**

```bash
git add tests/e2e/core-loop.spec.ts
git commit -m "test: add core browser smoke coverage"
```

## 任务 5：增加 bundle 体积观察

**文件：**
- 新增：`scripts/bundle-report.mjs`

- [ ] **步骤 1：新增 bundle report 脚本**

创建 `scripts/bundle-report.mjs`：

```js
import { existsSync } from 'node:fs'
import { readdir, stat } from 'node:fs/promises'
import { extname, join, relative } from 'node:path'
import { fileURLToPath } from 'node:url'

const distDir = fileURLToPath(new URL('../dist', import.meta.url))

const budgets = {
  largestJsBytes: 700 * 1024,
  largestCssBytes: 120 * 1024,
  totalBytes: 3 * 1024 * 1024,
}

function formatBytes(bytes) {
  if (bytes >= 1024 * 1024) return `${(bytes / 1024 / 1024).toFixed(2)} MiB`
  if (bytes >= 1024) return `${(bytes / 1024).toFixed(1)} KiB`
  return `${bytes} B`
}

async function collectFiles(dir) {
  const entries = await readdir(dir, { withFileTypes: true })
  const files = []
  for (const entry of entries) {
    const absolute = join(dir, entry.name)
    if (entry.isDirectory()) {
      files.push(...await collectFiles(absolute))
    } else if (entry.isFile()) {
      const info = await stat(absolute)
      files.push({
        path: relative(distDir, absolute).replaceAll('\\', '/'),
        ext: extname(entry.name),
        bytes: info.size,
      })
    }
  }
  return files
}

if (!existsSync(distDir)) {
  console.error('dist/ 不存在。请先运行 npm run build。')
  process.exit(1)
}

const files = await collectFiles(distDir)
const totalBytes = files.reduce((sum, file) => sum + file.bytes, 0)
const jsFiles = files.filter((file) => file.ext === '.js').sort((a, b) => b.bytes - a.bytes)
const cssFiles = files.filter((file) => file.ext === '.css').sort((a, b) => b.bytes - a.bytes)
const largestJs = jsFiles[0]
const largestCss = cssFiles[0]

console.log('Bundle 报告')
console.log(`dist 总体积：${formatBytes(totalBytes)}`)
if (largestJs) console.log(`最大 JS：${largestJs.path} (${formatBytes(largestJs.bytes)})`)
if (largestCss) console.log(`最大 CSS：${largestCss.path} (${formatBytes(largestCss.bytes)})`)
console.log('最大文件：')
for (const file of [...files].sort((a, b) => b.bytes - a.bytes).slice(0, 12)) {
  console.log(`- ${file.path}: ${formatBytes(file.bytes)}`)
}

const failures = []
if (largestJs && largestJs.bytes > budgets.largestJsBytes) {
  failures.push(`最大 JS ${formatBytes(largestJs.bytes)} 超过 ${formatBytes(budgets.largestJsBytes)}`)
}
if (largestCss && largestCss.bytes > budgets.largestCssBytes) {
  failures.push(`最大 CSS ${formatBytes(largestCss.bytes)} 超过 ${formatBytes(budgets.largestCssBytes)}`)
}
if (totalBytes > budgets.totalBytes) {
  failures.push(`dist 总体积 ${formatBytes(totalBytes)} 超过 ${formatBytes(budgets.totalBytes)}`)
}

if (failures.length > 0) {
  console.error('Bundle budget 失败：')
  for (const failure of failures) console.error(`- ${failure}`)
  process.exit(1)
}
```

- [ ] **步骤 2：构建后验证 bundle report**

运行：

```bash
npm run build
npm run bundle:report
```

预期：
- build 退出码为 0。
- bundle report 输出 dist 总体积、最大 JS、最大 CSS 和前 12 个最大文件。
- 在初始 budget 下，bundle report 退出码为 0。

- [ ] **步骤 3：提交任务 5**

```bash
git add scripts/bundle-report.mjs
git commit -m "chore: add bundle size report"
```

## 任务 6：记录并运行第一阶段质量门

**文件：**
- 修改：`README.md`

- [ ] **步骤 1：在 README 增加质量命令**

在 `README.md` 的快速启动命令块后增加：

````md
## 质量检查

```bash
npm run lint             # oxlint 静态检查
npm run test:regression  # Node + SSR 回归脚本
npm run test:e2e         # Playwright 浏览器烟测
npm run build            # TypeScript + Vite 生产构建
npm run bundle:report    # dist 产物体积报告
npm run quality          # lint + regression + build + bundle report
npm run quality:full     # quality + Playwright E2E
```
````

- [ ] **步骤 2：运行完整本地验证**

运行：

```bash
npm run lint
npm run test:regression
npm run build
npm run bundle:report
npm run test:e2e
```

预期：
- `npm run lint` 退出码为 0，且没有 warning。
- `npm run test:regression` 退出码为 0。
- `npm run build` 退出码为 0，并生成 `dist/404.html`。
- `npm run bundle:report` 退出码为 0。
- `npm run test:e2e` 退出码为 0，3 条测试通过。

- [ ] **步骤 3：提交前检查工作区**

运行：

```bash
git status --short
```

预期：
- 只出现本计划涉及的第一阶段文件。
- 预先存在且无关的用户改动保持未暂存，不能被回滚。

- [ ] **步骤 4：提交任务 6**

```bash
git add README.md
git commit -m "docs: document quality workflow"
```

## 最终验证

运行：

```bash
npm run quality:full
git status --short
```

预期：
- `npm run quality:full` 退出码为 0。
- `git status --short` 不再显示第一阶段未提交文件。
- 预先存在的无关用户改动可以继续保留，不能回滚。

## 自查记录

规格覆盖：
- 阶段 1 CI gate：任务 3 覆盖。
- 当前 lint warning 清理：任务 2 覆盖。
- 浏览器烟测：任务 4 覆盖。
- bundle 体积观察：任务 5 覆盖。
- 开发命令文档：任务 6 覆盖。

有意不做：
- Store slice 拆分从阶段 2 开始。
- AI gateway 从阶段 3 开始。
- 桌面产品布局从阶段 4 开始。
